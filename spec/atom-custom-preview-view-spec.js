'use babel';

import AtomCustomPreviewView from '../lib/atom-custom-preview-view';

describe('AtomCustomPreviewView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
